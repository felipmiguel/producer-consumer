// Root project build configuration
// The project is organized into multiple modules:
// - producer-consumer-core: Core library with producer-consumer framework
// - producer-consumer-samples: Sample applications demonstrating library usage

allprojects {
    group = 'com.batec'
    version = '1.0.0'
}